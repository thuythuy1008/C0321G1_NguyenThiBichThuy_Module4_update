package com.codegym.model.repository.employee;

import com.codegym.model.entity.employee.PositionEmpl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PositionEmplRepository extends JpaRepository<PositionEmpl, Integer> {
}
